describe('Product photos', () => {
  // aerial:examples
  // scenario: Customer can view photos of a product they are interested in.

  it('product image gallery loads', () => {
  });

  it('user can scroll the gallery left', () => {
  });

  it('user can scroll the gallery right', () => {
  });

  it('the gallery loops left', () => {
  });

  it('the gallery loops right', () => {
  });

  it('user can maximize selected photo', () => {
  });

  it('user can close maximized photo', () => {
  });

  // aerial:end
});
